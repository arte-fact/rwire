[package]
name = "fine-grained"
version = "0.1.0"
edition = "2021"

[dependencies]
rwire = { path = "../../rwire" }
rwire-themes = { path = "../../rwire-themes" }
async-std = { version = "1", features = ["attributes"] }
