# Sample workspace

Pick a file on the left. **Markdown renders**; code gets highlighted.
Hit *Edit* to switch to the editor: line numbers, dirty marks as you
type, and a Save button that only lights up when something changed.
