[package]
name = "rwire-canvas"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = "1.5"
tokio = { version = "1", features = ["net", "io-util", "macros", "rt-multi-thread", "time", "sync", "fs"] }
tokio-tungstenite = "0.24"
futures-util = "0.3"
