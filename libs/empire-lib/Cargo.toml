[package]
name = "empire-lib"
version = "0.1.0"
edition = "2021"

[dependencies]
rwire = { path = "../rwire" }
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
