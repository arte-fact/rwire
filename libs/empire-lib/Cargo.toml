[package]
name = "empire-lib"
version = "0.1.0"
edition = "2021"

[dependencies]
rwire = { path = "../rwire" }
rand = { version = "0.8", features = ["small_rng"] }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
