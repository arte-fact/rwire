[package]
name = "rwire-token"
version = "0.1.0"
edition = "2021"
description = "Declarative YAML apps on rwire"

[dependencies]
rwire = { path = "../rwire" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
